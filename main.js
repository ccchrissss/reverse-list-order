function reverseList(list) {

  return list.reverse()
}

console.log(reverseList([1, 3, 5, 7, 9]))

// params: list   // an array
// return list    // reversed array
// e.g. reverseList([1, 3, 5, 7, 9])
// results in [9, 7, 5, 3, 1]


// use reverse method on list and return the result